import 'mocha';
import {expect} from 'chai';
import {funcionObservar} from '../src/Ejercicio3/watchNotas';

describe('FilwWatcher', () => {
  it('Existe una clase de prueba', () => {
    expect(funcionObservar).to.exist;
  });
});